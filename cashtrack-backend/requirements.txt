fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-dotenv>=1.0
# Postgres driver for Supabase (switch DATABASE_URL to your connection string)
psycopg2-binary>=2.9
pytest>=8.0
httpx>=0.27
# Postgres driver — needed only when switching to Supabase (set DATABASE_URL):
# pip install psycopg2-binary
