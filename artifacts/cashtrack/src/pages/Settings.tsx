import { ScreenContainer } from '@/components/layout';

/** Placeholder -- settings screen content is not built yet. */
export function Settings() {
  return (
    <ScreenContainer>
      <span className="label-caps text-primary">Settings</span>
      <h1>Settings</h1>
    </ScreenContainer>
  );
}
