<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this is just the window background theme. -->
    <style name="Theme.CashTrack" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
